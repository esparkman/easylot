.row
  = form_for [:admin, announcement] do |form|
    .col-md-8
      .card
        .header
          %h4.title Create an Announcement
        .content
          %form
            = render 'form', form: form
          .clearfix
            = form.submit 'Save Announcement', class: 'btn btn-info btn-fill pull-right'
