.row
  .column
    %h1 Edit Vehicle
    = form_for vehicle, html: { multipart: true } do |form|
      = render 'form', form: form
      %p
        = form.submit 'Update Vehicle', class: 'success button'
